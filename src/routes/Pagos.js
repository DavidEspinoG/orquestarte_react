import { Template } from "../components/Template";
import React from "react";
const Pagos = () => {
  return(
  <>
    <Template pagos={true}>
      <h1>Pagos</h1>
    </Template>
  </>)
}

export default Pagos;